# 先进互联拓扑演进

## 引言

超节点作为AI集群的核心组成部分，已成为大模型训练、推理的基石，其内部互连拓扑直接决定通信时延、带宽效率、可扩展性与部署成本。本节梳理了互连拓扑的关键设计问题，以多层交换网络到低直径直连网络的演进思路展开，依次阐述CLOS拓扑、Dragonfly拓扑、Dragonfly+拓扑、3D-Torus拓扑及Slimfly等新兴直连拓扑的核心性质与适用场景，为超节点的互连拓扑设计提供参考。

## 拓扑设计的关键问题

### Radix受限下的拓扑规模问题

互连拓扑本质上是带约束的图构建问题：设互连拓扑为图 $G = (V, E)$，$V$ 为节点集合（含交换机、服务器），$|V| = N$ 为网络规模，$\Delta(G) \leq k$ 为节点Radix端口数约束，$D(G)$ 为跳数直径。在端口数 $k$ 固定的约束下，最大化网络规模 $N$，同时维持较小的跳数直径 $D$ 以保障高带宽与低时延。该问题受经典 degree–diameter bounds 的约束，由摩尔界（Moore bound）给出规模理论上限：

$$
M_{\Delta, D} = 1 + \Delta + \Delta(\Delta-1) + \Delta(\Delta-1)^2 + \cdots + \Delta(\Delta-1)^{D-1} \geq N
$$

该理论上界由端口数与网络直径两个指标双重约束。若想打破该上界约束扩大规模，有两个方向：

- **方向一**：引入多层交换网络，增加网络成本，典型结构是CLOS、Dragonfly拓扑。
- **方向二**：通过增加节点端口数构建低直径直连拓扑，降低跳数与时延，同时通过降低的网络成本用于提高算力密度提升性能，典型结构是Torus、Slimfly等拓扑。

### 带宽、成本与时延之间的权衡

AI大模型的高带宽需求推动了CLOS/Fat-Tree架构的应用，通过增加额外的交换端口实现互连转发，从而避免端侧转发导致的带宽损失。但此类结构由于增加了网络层级，成本、跳数与时延往往高于低直径直连网络，在时延敏感的小包通信中效果不佳。同时规模增长会受限于大Radix交换机的先进芯片制程约束。

低直径直连拓扑设计则通过减少数据流经过的交换设备级数，在降低延迟与功耗的同时维持高带宽，从而提升每比特传输的性价比。但另一方面，这类设计对路由选择策略与流量疏导需通过精细化设计以平衡性能与稳健性。

### 混合静态与动态交换架构（OCS）

光交换（OCS）技术引入动态可变的"动态边"后，可简化部署，提高拓扑的灵活性。互连拓扑不再是单一静态图，而是由静态子图与可动态调整子图组成的拓扑序列。此时网络设计转化为联合优化问题：

- 设计稳定的基础静态拓扑，满足最坏场景下的流量承载与故障容错需求；
- 优化可重构链路分配策略，匹配任务变化时的流量需求。

## 拓扑演进理念：从多层交换网络到低直径直连网络

高性能网络拓扑的演进本质上是**在网络直径（时延）、带宽效率（吞吐）与建设成本（Capex/Opex）之间寻求最优平衡的过程**。随着 AI 算力规模的爆炸式增长，拓扑架构正经历从“分层交换”向“低直径直连”的范式转移：

* **分层交换的基石——[CLOS 架构](../01-architecture/topology.md#clos-network-structure多级交换网络架构)**：
  通过多级交换机的对称堆叠实现严格无阻塞转发。它是带宽敏感型流量的首选，但在超大规模部署时，交换机与光模块的物料成本（Capex）及功耗（Opex）会随层数增加而呈指数级上升。
* **规模与成本的平衡——[Dragonfly 系列](../01-architecture/topology.md#dragonfly-拓扑)**：
  引入“组间全互联”设计，通过降低网络层级和减少全局链路，在万卡集群中实现了高性价比的低跳数通信。其中 **[Dragonfly+](../01-architecture/topology.md#dragonfly-拓扑-1)** 更是结合了组内 CLOS 的灵活性与组间直连的高效性，是当前大规模 AI 集群的重要演进形态。
* **去中心化的极致——[3D-Torus 架构](../01-architecture/topology.md#3d-torus-拓扑)**：
  彻底舍弃了额外的交换设备，利用计算节点通过多维环面直接互联。这种“零交换机”的架构在邻居通信（如 AllReduce）中表现出极低的时延与极致的性价比，是特定大规模算力平台（如谷歌 TPU）的核心选择。
* **理论最优的终极形态——[Slimfly 架构](../01-architecture/topology.md#slimfly-拓扑)**：
  作为下一代拓扑的参考设计，Slimfly 基于最优直径图理论，将网络直径固定为 2。它在同等端口约束下能连接最多的节点，平衡了直连拓扑的低成本与分层拓扑的高适应性，在时延、功耗及布线复杂度上均逼近物理极限。

---